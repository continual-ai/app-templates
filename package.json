{
  "name": "app-templates",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm check:react-blocks && pnpm check:visual-drift && pnpm -r check",
    "check:react-blocks": "node scripts/check-react-blocks.mjs",
    "check:visual-drift": "node scripts/check-visual-drift.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
