{
  "name": "@continual/react-app-site",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "pnpm --dir frontend dev",
    "build": "pnpm --dir frontend build",
    "preview": "pnpm --dir frontend preview",
    "check": "pnpm -r check"
  }
}
